# Configuration Base de Données PostgreSQL
# Fichier de configuration pour l'application de réservation

# Paramètres de connexion
DB_HOST=192.168.1.46
DB_PORT=5432
DB_NAME=slam_reservation_vehicule
DB_USER=rayan
DB_PASSWORD=rayan789

# Instructions pour modifier la connexion :
# 1. Éditez ce fichier avec vos paramètres
# 2. Modifiez le fichier src/Passerelle.java avec ces valeurs
# 3. Recompilez avec : bash compile.sh
# 4. Lancez avec : bash run.sh

# Exemple de configuration locale :
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=ma_base
# DB_USER=mon_utilisateur
# DB_PASSWORD=mon_mot_de_passe

# Pour tester la connexion PostgreSQL depuis le terminal :
# psql -h 192.168.1.46 -p 5432 -U rayan -d slam_reservation_vehicule
